services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: restrox-backend
    ports:
      - "5000:5000"
    env_file:
    - ./server/.env
    environment:
      NODE_ENV: development
      PORT: 5000
    restart: unless-stopped
